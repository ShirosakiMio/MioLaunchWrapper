plugins {
    id 'java'
}

group = 'mio'
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
}

jar {
    manifest {
        attributes(
                'Add-Opens': 'java.base/jdk.internal.loader',
                'Main-Class': 'mio.Wrapper'
        )
    }
}